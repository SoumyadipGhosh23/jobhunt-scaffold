{
  "name": "jobhunt",
  "version": "1.0.0",
  "private": true,
  "description": "Daily job feed: pulls from ATS boards + aggregators, filters, ranks against CV, commits a markdown report.",
  "type": "module",
  "scripts": {
    "verify": "tsx src/main.ts verify",
    "dry-run": "tsx src/main.ts dry-run",
    "run": "tsx src/main.ts run"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
